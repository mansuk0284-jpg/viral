# Viral Monitor — 혼수 가전 바이럴 모니터링 하네스

온라인 혼수가전 **구매후기**를 조사·분석하여, **전국 백화점에 입점한 삼성스토어 vs LG전자 매장**의
바이럴(구매후기) 현황을 매장·지역 단위로 비교하고, **삼성스토어가 LG보다 혼수 판매를 늘릴 인사이트**를 찾는다.

## 목적 (2026-06 개정 — 백화점 입점 매장 중심으로 재정의)

1. **대상 = 전국 백화점 입점 삼성스토어 vs LG전자 매장**의 혼수가전 구매후기 비교.
   - 비교 단위: ①전국 ②지역별 백화점 ③백화점 내 삼성 vs LG. 다양한 시각으로 드릴다운.
2. **모니터링 후기 = 혼수가전 구매후기**. "웨딩가전·신혼가전·혼수가전·가전 졸업" 등 표현이 다양하나, 결국 **결혼하며 구매하는 가전 후기**를 가리킨다.
3. **백화점 입점 매장 외 데이터는 분석에 사용하지 않는다(보관만).** 삼성스토어 단독매장·대리점·하이마트·온라인몰 등은 census에 보관하되 본 분석/대시보드 집계에서는 제외.
4. 목표 = LG 대비 우위·열세 진단 → **삼성스토어 혼수 판매 증대 인사이트** 도출.

## 백화점 매장 명칭 규칙 (집계 핵심)

- 후기에서 매장은 **백화점명 + 브랜드** 조합으로 불린다.
  - 백화점: 롯데·신세계·현대·갤러리아·AK 등 (약칭 롯/신세/현/갤 포함). 예) 롯데백화점 잠실점 → **롯데잠실 / 롯잠실 / 롯본점(본점)**.
  - 브랜드: 삼성·삼·삼성스토어 / LG·엘지·엘·하이프라자·베스트샵.
  - 예) "롯데잠실 삼성", "롯잠실 엘지", "신세계 강남 LG", "현대 판교 삼".
- 매장 키 = **백화점지점 + 브랜드(삼성/LG)**. 같은 백화점 지점에서 삼성 후기 vs LG 후기를 비교한다.
- 백화점 단서가 없는 후기는 "백화점 미상"으로 분류(분석 제외, 보관).

## 에이전트 팀

> **임무·경계 단일 진실원천: [`data/agent-roster.md`](../../data/agent-roster.md)** — 누가 무엇을 하는지, 계층(수집→분류→매칭→검증→시각화·검수)과 중복 방지 경계를 한 표로 정리. 아래는 요약.

### 채널 전담 수집 에이전트 (히어로 6타일 1:1)

| 에이전트 | 스킬 | 채널 / 소스ID | 수집 방식 |
|---------|------|------|------|
| dagyeolun-collector | collect-dagyeolun | 다이렉트결혼준비 / dagyeolun | 로컬 스크래퍼(정본·실데이터, board menuId 280) |
| blog-collector | collect-blog | 네이버 블로그 / naver-blog | 네이버 검색 API → `naver_api_collect.py`(제목+요약+`--read-body` 본문) |
| momcafe-collector | collect-momcafe | 부울경 맘카페 / busan-mom-cafe | 네이버 카페 검색 API → `naver_api_collect.py`(권역 필터+`--read-body`) |
| youtube-collector | collect-youtube | 유튜브 / youtube | YouTube Data API v3 → `youtube_collect.py`(영상+댓글) |
| ohou-collector | collect-ohou | 오늘의집 / ohou | **수집 불가 — 로그인해도 전 경로 `Access Denied`**(2026-08-24 실측: 콘텐츠·스토어검색·조언·모바일 네 경로 모두). 자동화 차단이다 — 타일 건수를 지어내지 않는다 |
| insta-collector | collect-insta | 인스타그램 / instagram | `insta_login.py`(사용자 직접 로그인) → `collect_instagram.py` — 혼수 해시태그 44종. 태그 URL 은 검색 페이지로 리다이렉트되므로 `/explore/search/keyword/?q=%23...` 로 바로 간다. **절반이 판매자 홍보 글**이라 `biz` 로 갈라 담는다 |

각 채널 에이전트는 `01-raw-{소스ID}.md`를 산출하고, `review-collector`가 이를 `01-raw-reviews.md`로 병합한다. 채널↔에이전트 매핑 상세는 `data/sources.md`.

### 채널 접속(로그인) 엔지니어

| 에이전트 | 스킬 | 역할 |
|---------|------|------|
| channel-access-engineer | channel-access | 채널별 로그인·접속 문제 해결. 사용자 직접 로그인 세션을 `.browser-profile`에 저장·점검(`scripts/channel_session.py`)하고 로그인 후 검색 진입을 열어, 공개검색의 빈약한 표본을 넘어서게 한다. **비번은 코드가 절대 입력 안 함.** 채널 수집가는 "로그인 세션 우선, 없으면 공개검색 폴백". |

### 제휴카페 전담 (혼수 채널과 잣대가 다름)

| 에이전트 | 스킬 | 역할 |
|---------|------|------|
| affiliate-cafe-analyst | affiliate-cafe-insight | 회사 제휴카페 84곳(1,136만명)을 **지역 생활 커뮤니티**로 해석. 혼수 후기 건수가 아니라 ①교체·이사 수요 ②경쟁 유통(전자랜드·하이마트) 지역 마케팅 노출 ③구매·추천 상담(미결정 고객) ④우리 제휴 활동 ⑤중고 거래 ⑥구독·렌탈 ⑦사후서비스 ⑧온라인구매 8축으로 인사이트를 낸다. 화면은 `web/affiliate-view.js`(좌=가전 전반 / 우=당사, 월 단위·첫 진입은 현재 월). **다이렉트결혼준비와 수치 합산 금지.** 대상 목록·clubId = `artifacts/affiliate-cafes.json` |


### UI 공용 규칙 (새 페이지를 만들 때 먼저 읽는다)

| 스킬 | 무엇 |
|---|---|
| ui-navigation(글자) | **`web/text-fit.css`** — 수치·이름·버튼이 두 줄로 갈라지지 않게 하는 공용 규칙. 전체화면 모드가 글자만 키우고 칸 폭은 그대로 둬서 `100%`가 "100/%"로, `롯데 부산본점`이 두 줄로 갈라졌다(2026-08-21 실측). 화면별 CSS **뒤에** 링크해야 이긴다 |
| ui-navigation(글자2) | **`web/text-fit.js`** — 이름을 말줄임(…)하지 않고, 폭은 고정한 채 글자를 줄여 전부 보여준다. "신세계센텀…" 으로 자르면 어디인지 알 수 없다(2026-08-21 사용자 지적). 하한 9.5px, MutationObserver 로 자동 적용 |
| ui-navigation | 화면 이동(앞/뒤/처음)과 기간 선택. `web/nav.js`(window.VNAV) 사용. **화면마다 뒤로가기를 새로 만들지 않는다** — 만들었더니 버튼이 겹치고 라벨이 동작과 어긋났다(2026-08-14 실측) |


### 네이버 리뷰·예약 전담 (세 번째 잣대)

| 에이전트 | 스킬 | 역할 |
|---------|------|------|
| naver-review-analyst | (agents 파일에 절차 포함) | 네이버 플레이스 **방문자 리뷰**로 매장을 본다. 혼수 후기(구매)·제휴카페(생활)와 달리 **실제 방문 고객의 평가**다. 리뷰 건수·칭찬 키워드(네이버 집계)·아쉬움 원문·방문사유·매니저 실명·예약 경유(추정)를 같은 상권 LG와 나란히 비교. **예약 '건수'는 외부 조회 불가 — 지어내지 말고 추정으로만.** 대상 = `artifacts/naver-place/*.json`, 화면 = `web/naver-review-view.js` |


### 경쟁력 자료 (사내 실적 — 취급 주의)

| 스킬 | 무엇 |
|---|---|
| 매장 명부(SSOT) | **`data/백화점 리스트.xlsx` → `artifacts/compete.json` = 양사 입점 71곳**. 네이버 수집 이름은 `scripts/place_gate.py` 의 `TO_ROSTER` 로 명부 이름에 맞춘다. 거르는 규칙(모바일 MX·백화점 밖 단독점·중복 플레이스)도 여기 한 곳에만 둔다 — 화면과 리포트가 각자 걸러서 **119곳/130곳으로 갈라진 적이 있다** |
| compete-insight | `data/백화점 리스트.xlsx` 의 삼성 vs LG 경쟁력(당사÷X사 매출 배수). **금액 절대 미노출**, MX(모바일) 제외, 양사 입점 매장만. 화면엔 **hover 로만** 띄운다. 바이럴과의 상관은 r=+0.19 로 **거의 없다** — 대신 사분면으로 '말은 도는데 안 팔림'/'잘 파는데 안 알려짐'을 뽑는다 |

### 분석·집계 에이전트

| 에이전트 | 스킬 | 역할 |
|---------|------|------|
| review-collector | review-collect, naver-cafe-survey | 채널 산출물 병합 + 기타 카탈로그 소스 범용 수집 |
| brand-classifier | brand-classify | 삼성/LG 브랜드·품목 분류, 긍·부정 톤 |
| store-matcher | store-match | 권역 백화점 매장 매칭, 매장별 건수 집계 |
| trend-analyst | trend-analyze | 혼수 가전 트렌드 분석 |
| insight-validator | result-verify | 집계 결과 검증(QA) — 리포트 직전 부풀림·편향·산술오류 감사 |
| report-builder | report-build | 매장별 삼성vsLG 비교표 + 종합 리포트 |
| sales-growth-strategist | sales-growth-insight | 리포트 결과 → 삼성스토어 매출성장 실행 액션(P6) 변환 |
| geo-viz-designer | (geo-status-map) | 지도 위 데이터 표현(코로플레스·라벨·말풍선) 설계·구현. 라벨 겹침·글씨색 대비·작은 지역 처리 등 지도 표기 품질 담당 |
| instruction-steward | — | 사용자 다항목 지시를 체크리스트로 종합하고 각 항목을 실제 검증해 완료/미완/주의로 정직 보고(누락·과장 방지) |

## 진입 방법

```
"혼수 바이럴 모니터링 시작" 또는 "/viral-monitor-orchestrator"
"이번 주 부산 삼성스토어 바이럴 분석해줘"
```

## 소스 선택

- 모니터링할 사이트는 `data/sources.md` 카탈로그에서 **복수 선택**한다 (카페·블로그·커뮤니티·SNS).
- 세션 시작 시 사용자에게 소스를 고르게 하고, 선택한 소스만 수집·집계한다.
- 결과는 **소스별로 분리**해 보여준다 (사이트마다 표본·성향이 달라 합산 해석 주의).

## 공통 규칙

- **분석 범위 = 2단 구조(2026-06 재조정).** ①**전국·지역 = 전체 후기 대상**으로 센싱(백화점 포함 모든 유통). ②**지역에서 매장을 특정할 때만 백화점 입점(롯데·신세계·현대·갤러리아·AK) 삼성스토어 vs LG전자**로 비교 — 그래야 삼성 vs LG 구도가 성립. census 전량은 보관.
- 매장 키 = **백화점지점 × 브랜드(삼성/LG)**. 명칭 약칭(롯잠실·롯본점·신세 강남 등)도 정규화해 같은 지점으로 묶는다.
- 모든 산출물은 `artifacts/` 아래에 `YYYYMMDD-` 접두사로 저장. 전수 census = `artifacts/cafe-census.json`(백화점 외 포함 보관), 분석용 = 백화점 필터 적용분.
- 웹 검색 결과는 **출처 URL을 반드시 함께 기록**한다 (검증 가능성 확보).
- 백화점 단서가 불명확한 후기는 "백화점 미상"으로 분류(분석 제외, 버리지 않고 보관).
- 집계는 추정임을 명시한다. 표본은 전수가 아니므로 "표본 기준 추정치"로 표기. 제목기반 추정과 본문확정을 구분 표기.


## 작업 후 되돌려 적기 (필수)

이 하네스가 계속 발전하려면 **한 번 배운 것이 파일에 남아야** 한다.
작업이 끝나면 아래를 확인하고, 어긋난 것이 있으면 그 자리에서 고친다.

| 확인 | 어디에 |
|---|---|
| 새로 만든 스크립트·산출 경로 | 해당 스킬의 실행 절차 |
| 함정·실패담(왜 그렇게 하면 안 되는지) | 해당 에이전트의 "배운 것" 절 |
| 분류 기준·축이 바뀌었나 | 스킬 + 에이전트 + 이 파일 + `data/agent-roster.md` **네 곳 모두** |
| 화면 원칙이 생겼나 | 스킬의 화면 절 |
| 사용자가 정정해 준 사실 | 메모리(`memory/`)에도 남긴다 |

**실제로 겪은 문제(2026-08-12):** 제휴카페 작업을 마쳤는데 스킬·에이전트 파일에
`build_affiliate_web.py`도 웹 화면도 월 단위도 적혀 있지 않았다. 축은 8개로 늘렸는데
문서는 5축 그대로였다. 이 상태로 다음 사람이 스킬만 읽으면 **리포트만 만들고
화면은 옛 수치를 계속 보여준다.** 코드와 문서가 갈라지면 하네스는 껍데기가 된다.

> 판단 기준: **"맥락을 모르는 사람이 이 파일만 읽고 같은 결과를 낼 수 있는가."**
> 아니라면 그 차이가 곧 빠진 내용이다.

## 메모리 위치

```
C:\Users\admin\.claude\projects\C--Users-admin-Desktop-viral\memory\
```

## 후기의 주체는 고객이다 (2026-08-23 사용자 지적)

**후기는 고객이 쓴다. 우리가 쓰거나 만들거나 늘리는 것이 아니다.**
매니저(직원)가 할 수 있는 일은 **구매 고객이 후기를 쓰도록 요청·독려하는 것**뿐이다.

액션 문구를 쓸 때 주체를 틀리지 않는다:

| 쓰지 말 것 | 쓸 것 |
|---|---|
| 후기 절대량을 키우세요 | 구매 고객에게 **후기 작성을 요청**해 표본을 넓히세요 |
| 후기를 늘리는 것이 과제 | **후기 요청**을 습관화하는 것이 과제 |
| 좋은 후기 만들기 | 좋은 후기가 **나오려면** |
| 후기 확보를 지속 | **고객 후기 요청**을 이어가기 |

바이럴 지표가 낮다는 것은 "우리가 글을 덜 썼다"가 아니라
**"고객이 우리 이야기를 덜 남겼다"** 는 뜻이다. 그래서 처방도 상담 품질과
후기 요청 습관으로 간다 — 매니저 실명이 후기에 남도록 요청하는 것이 대표적이다.

## 채널별 현황과 매장별 현황은 같은 데이터다 (2026-08-23 사용자 정리)

> "채널별 현황은 채널별 타일에 데이터를 정리하는 거잖아. 그리고 매장별 현황은
> 해당 매장이 나오는 데이터를 채널별로 정리한거고. 바라보고 접근하는 시각이 틀릴뿐
> 결국은 매장별 현황, 채널별 현황은 그 데이터가 서로 연동되어 있어."

```
        같은 자료
       ↙        ↘
  채널 축        매장 축
 (채널 타일)   (매장 대시보드)
```

- **채널 축** — 타일 하나 = 채널 하나. 그 채널 안에서 브랜드·품목·매장·담당자를 정리한다.
- **매장 축** — 매장 하나를 놓고 **채널마다 카드 하나**. 그 매장이 각 채널에서 어떻게 보이는지.

**새 채널을 붙일 때는 반드시 둘 다 만든다.** 하나만 만들면 화면이 갈라진다.

| 만들 것 | 어디 |
|---|---|
| 채널 데이터(두 축) | `scripts/build_{채널}_web.py` — `total/s/l/months/items` + `stores{매장: {s,l,mgr,last}}` |
| 히어로 타일 | `web/index.html` 의 `#sourceMosaic` + `data-go` 분기 |
| 채널 화면 | `web/{채널}-view.js` — `window.open{채널}()` |
| 매장 카드 | `web/store-scope.js` 의 `CHANNELS` 배열 + `{채널}Card()` |

**매장 이름은 `build_web_data.dept_store_of()` 를 재사용한다.** 채널마다 따로 뽑으면
같은 매장을 다른 이름으로 불러 두 화면이 어긋난다.

**표본 크기를 숨기지 않는다.** 제이웨딩 512건이 다이렉트웨딩 84,419건 옆에 같은 크기로
놓이면 같은 무게로 읽힌다. 화면에 "표본이 훨씬 작습니다"를 적고,
그 채널의 **강점**(제이웨딩은 매장·담당자 지목)에 무게를 두게 안내한다.

## 단위마다 묻는 것이 다르다 — 전국 / 도시 / 매장 (2026-08-23 사용자 지시)

> "전국, 도시, 매장별 페이지의 분석 내용과 분석의 밀도가 차이가 있어야 한다 …
> 전국은 좀 더 큰 시각에서, 매장의 경우 세밀한 부분까지."

실측해 보니 **도시 화면이 매장 화면의 축소판**이었다 — 같은 카드(품목·혜택·추이,
계약 규모·리스크)를 둘 다 쓰고 있었다. 단위만 다르고 시각은 같았던 셈이다.

| 단위 | 묻는 것 | 화면에 두는 것 |
|---|---|---|
| **전국** | 구조가 어떤가 | 전체 규모·지도 · 우위/열세를 만든 구조 · 품목 경쟁 지형 · 매니저 실명 · 몇 개점 우위 |
| **도시** | 어디쯤이고, 안에서 얼마나 갈리나 | 표본 순위·전국 대비 · **매장별 순위(주인공)** · 도시 안 격차 · 쏠림 · 전국과 다른 점 |
| **매장** | 무엇을 어떻게 할까 | 품목·혜택·추이 · 계약 규모 · 매니저 실명 · 현장 액션 · 후기 원문 |

**금지: 매장급 디테일(품목별 처방·객단가)을 도시에 두지 않는다.**
도시에서 그걸 보면 매장 목록이 눌린다 — 실제로 진단이 529px 을 먹어
매장 목록에 123px 만 남았고, 서울 20곳이 **행 높이 6px** 로 찌부러졌다.

### 공간은 주인공에게 준다

도시 화면의 주인공은 **매장 목록**이다. 실측으로 잡은 배분:
- 진단 `max-height: 54%`, 목록 `min-height: 170px`
- 매장 12곳 초과면 목록을 **2열**로(서울 20곳 → 행 높이 6px → 32px)
- 매장이 많은 도시는 진단을 **2줄**로, 적으면 3줄
- 기회 카드는 **걷어냈다** — 바로 옆 매장 목록이 같은 것을 더 자세히 보여준다(중복)

도시 액션은 한 수만 짚고 `→ 품목·혜택별 처방은 매장을 눌러 확인하세요` 로 넘긴다.

## 채널 줄에 무엇을 두는가 (2026-08-24 사용자 지적)

> "채널별 현황 페이지에서 혼수기반 바이럴 채널에 왜 네이버 리뷰가 있는거야?
> 네이버 리뷰는 혼수기반 바이럴이 아니야. 네이버 리뷰는 매장별 현황에서
> 각 매장의 네이버 리뷰를 살펴보는 거야."

**혼수 기반 바이럴 채널 = 혼수 준비 중에 오가는 가전 이야기.** 그것만 이 줄에 둔다.

| | 무엇 | 어디에 |
|---|---|---|
| **본류** | 웨딩카페 후기 — 다이렉트결혼준비 · 제이웨딩 · 블로그 · 맘카페 | 채널 줄 |
| **넓은 채널** | 유튜브 · 오늘의집 · 인스타 — 파급이 커서 **그 안에서 혼수가전만 골라** 본다 | 채널 줄 |
| **매장 평가** | 네이버 리뷰·예약 — 방문자 평가이지 혼수 후기가 아니다 | **매장별 현황에만** |

네이버 리뷰는 성격이 다르다. 매장 대시보드에서 **그 매장의 리뷰**를 보는 것이 제자리다
(카드를 누르면 그 매장 리뷰 상세로 들어간다). 채널 줄에 두면 혼수 바이럴 합계에
방문자 평가가 섞여 숫자의 뜻이 흐려진다.

**히어로의 '누적 N건 센싱'은 이 줄에 있는 타일의 합**이다 — 그래서 네이버 리뷰를 빼자
84,955건(혼수 채널만)이 됐다.

## 분석 화면은 인사이트로 끝나야 한다 (2026-08-24 사용자 지시)

> "분석된 페이지는 인사이트가 있어야해. 삼성스토어에서 참고할 만한 내용이 있어야 해.
> 사전적 의미로만 참고할 내용 등으로 단락을 만들지는 마."

**실측으로 확인한 문제:** 행동 지시 문장 수가 다이렉트결혼준비 28건 vs
나머지 여섯 화면 **0건**이었다. 그 자리를 채우고 있던 것은 지표의 사전적 정의였다.

| 쓰지 말 것 (사전적 의미·읽는 법) | 쓸 것 (그래서 무엇을) |
|---|---|
| 리뷰 수는 방문 고객이 남긴 **흔적의 양**입니다 | 상대는 '배송이 빨라요'로 칭찬받는데 우리 매장엔 이 키워드가 없습니다 — 상담에서 그 대목이 비어 있습니다 |
| 몇 편을 올렸나와 몇 번 보였나는 다릅니다 | 냉장고·TV·청소기는 **LG 영상이 더 많습니다** — 비교 질문에 답할 준비를 해두세요 |
| 비율은 참고로 보고 매장·담당자 지목에 무게를 두세요 | 삼성 후기는 있는데 **담당자 이름이 한 번도 안 나온 매장 7곳** — 후기 요청이 습관으로 자리 잡지 않은 곳 |
| 표본이 작습니다 (그것만) | 매장명이 적힌 글 5곳이 **하이마트·LG가 올린 홍보** — 경쟁이 인스타로 고객을 부르고 있습니다 |

**판별법:** 승패가 뒤바뀌어도 같은 문장이 나온다면 그것은 인사이트가 아니다.
네이버 리뷰 진단문이 이기든 지든 "흔적의 양입니다"로 똑같았다 —
그 매장 고유의 정보가 0이었다는 뜻이다.

방법론 고지(표본 기준 추정치·예약 건수 외부 조회 불가 등)는 **여전히 필요하다**.
다만 그것'만' 남으면 안 된다. 화면마다 `현장 액션` 카드(`.yt-act`)를 둔다.

### 숫자를 문장으로 만들 때의 규칙

| 규칙 | 왜 |
|---|---|
| **표본 10건 미만이면 퍼센트를 적지 않는다** | 9건으로 "삼성 100%"라고 쓰면 한 건만 뒤집혀도 89%가 된다 |
| **편차 0p는 적지 않는다** | "지역평균보다 0p 높습니다"는 아무 말도 아니다 |
| **실행 불가능한 목표는 목표로 걸지 않는다** | 격차 7,208건에 "월 601건씩"은 사실이지만 액션이 아니라 포기 사유가 된다. 누적을 못 뒤집을 땐 **새로 쌓이는 속도**로 목표를 바꾼다 |
| **조사(은/는)를 받침으로 고른다** | "인덕션는"은 회사 전체가 보는 화면에서 바로 눈에 걸린다. `josa()` 사용 |
| **주체를 상호로 확정한다** | 롯데 잠실 글은 "LG가전 BIG SALE"인데 본문에 '삼성'이 스쳐서 우리 홍보로 잡혔다. 브랜드 언급이 아니라 **상호**(삼성스토어/하이마트/베스트샵)로 갈라야 한다 |

## 화면 여백과 활자 (2026-08-24 사용자 지시)

> "좌우 많이 넓고 하단 자리없고, 위는 적당히 공간이 있어" / "글씨크기를 조금 줄이는게 좋을 것 같고"

실측(1440×900): 좌 47 · 우 47 · 상 22 · 하 **14** — 좌우가 하단의 3.4배였다.
글씨는 9px~57px **스물여섯 가지**로 흩어져 있었다.

**정한 값 — `web/analysis-ui.css` 한 곳에만 둔다.**

| | 값 |
|---|---|
| 여백 | `#channel.section { padding: 26px 0 }` + `.container { max-width: min(1600px,98vw); padding: 0 14px }` → 사방 26~28px |
| 활자 계단 | `--an-title:26 / --an-hero:46 / --an-big:42 / --an-num:32 / --an-mid:19 / --an-body:17 / --an-sub:12.5` |

- `analysis-ui.css`는 **화면별 CSS 뒤에** 링크해야 이긴다.
- `body.view-cafe` 폰트 규칙 267줄이 토큰을 특이도로 이기고 있었다 —
  값을 그 자리에 또 적지 말고 **`var(--an-*)`를 가리키게** 한다.
- 통일 목록에 `.af-title`(제휴카페·네이버리뷰)이 빠져 제목만 38px이었다. 새 화면을 만들면 이 목록에 넣는다.

## 수집 범위 = 2026년 (2026-08-24 사용자 지시)

> "이제부터 수집하는 데이터는 26년 데이터만 수집하도록 해. 대신 최대한 끌어오도록 하고"

**앞으로 새로 수집하는 것은 2026년치만.** 이미 모은 과거분(2021~2025)은
그대로 두고 화면에도 계속 쓴다 — 지우는 것이 아니라 **더 캐지 않는 것**이다.

| 어디 | 어떻게 |
|---|---|
| `enrich_census.py` | `--since` 기본값 `2026-01`. 과거를 열려면 명시해야 한다 |
| `collect_history.py` | `--start 2026-01 --end 2026-08` 로 돌린다 |
| 채널 수집(유튜브·인스타) | 검색이 기간을 못 고르므로 **수집은 전량, 화면에서 기간 탭으로 2026을 본다** |

### "최대한"은 쿼리를 넓히는 것이다

기간을 좁힌 만큼 그 안을 깊게 파야 한다. `collect_history.py` 는
월별 윈도우 × 쿼리 union 이라 **쿼리 수가 곧 수확량**이다.
2026 재수집 때 14개 → 34개로 늘렸다(품목·브랜드명·유통·혼수 표현 추가).

```
가전,후기,삼성,엘지,LG,냉장고,세탁기,건조기,에어컨,TV,티비,청소기,디오스,비스포크,
스타일러,김치냉장고,인덕션,식기세척기,정수기,오브제,트롬,그랑데,무풍,에어드레서,
워시타워,광파오븐,혼수,신혼,웨딩,가전졸업,견적,계약,백화점,하이마트
```

**같은 달을 다시 훑으려면 `.done` 에서 그 달을 지워야 한다** —
`artifacts/cafe-census.json.done` 에 완료월이 적혀 있고, 적힌 달은 건너뛴다.
articleId 로 중복을 거르므로 다시 훑어도 기존 글이 늘어나지는 않는다.

## 화면 이동은 브라우저에 맡긴다 (2026-08-24 사용자 지시)

> "페이지 이동 버튼은 이제 필요없는거 같아. 브라우저의 기능을 사용하게 됨에 따라"

화면 안의 `처음/뒤로/앞으로` 버튼(`VNAV.bar()`)은 **전부 걷어냈다.**
브라우저의 뒤로/앞으로가 같은 일을 하고, 그쪽이 사용자에게 이미 익숙하다.

**다만 `VNAV.push()` 는 그대로 둔다.** 이게 없으면 화면을 열어도 히스토리에
쌓이지 않아 뒤로가기가 **사이트를 떠나 버린다**. 버튼만 없앤 것이지
히스토리 연동을 없앤 것이 아니다.

새 화면을 만들 때: `VNAV.push({id, label, open})` 는 호출하고, `VNAV.bar()` 는 넣지 않는다.

## 기간 UI 는 `VPER.bar()` 를 쓴다 (2026-08-24)

사용자가 "유튜브·인스타에 기간 탭이 없다"고 했는데 **실은 있었다.**
`html()` 만 붙여 놓아 146px 짜리 칩 두 개가 화면 오른쪽 끝(x=1265)에 붙어 있었고,
다이렉트웨딩은 366px 에 현재 기간 라벨까지 있었다 — 같은 기능인데
눈에 띄는 정도가 달라 **없는 것처럼 보였다.**

`VPER.bar()` 를 쓰면 `현재 기간 라벨 + 칩` 이 함께 나와 무게가 같아진다.
새 화면은 `per().bar()` 를 쓴다. `html()` 은 라벨을 따로 그리는 화면에서만.

## 채널 화면 활자 (2026-08-24)

`body.view-cafe` 에는 활자를 키우는 규칙이 267줄 있는데 채널 화면(`body.view-cx`)에는
없어서 같은 클래스가 화면마다 15% 씩 갈렸다(.fc-plain 19.5 vs 17, 본문 11 vs 11.5).
`analysis-ui.css` 끝에 `body.view-cx` 계단을 두어 맞췄다.
**새 클래스를 만들면 두 곳 모두에 넣는다.**

## 카드 상세 색 원칙 — 긍정 파랑·부정 빨강 (2026-08-25 사용자 지시)

> "카드를 클릭하고 들어가면 긍정내용은 파란색, 부정적 내용은 빨간색으로 기본 원칙으로 작성"

`<b class="warn">`가 부정을 표시하는 관례는 이미 있었지만, **컴포넌트마다 색을
따로 정의**해 짝(`.warn` 오버라이드)이 없는 곳이 있었다. 실측: `.ca-reasons li span b`,
`.fc-sec.tip p b`는 자기 색(파랑)만 있고 `.warn` 대응이 없어 "오븐 39%"(부정)가
파랗게 보였다.

**고친 법 — `web/cafe-analysis.css` 맨 끝에 한 번만 못박는다:**
```css
.ca2 .fc-dbody b.warn, .ca2 .ca-reasons b.warn, ... { color: #d23b54 !important; }
```
새 카드·새 문장을 추가할 때 `.warn` 클래스를 붙이기만 하면 어디서든 빨강이 이긴다 —
컴포넌트별로 `.warn` 대응 색을 또 정의할 필요가 없다. `!important`는 이 규칙에 한해
의도적으로 쓴다(전역 원칙이 개별 컴포넌트 색보다 항상 이겨야 하므로).

## 숫자를 나란히 붙이지 않는다 (2026-08-25)

`<i>82%</i><em>977</em>`처럼 단위 없는 숫자를 퍼센트 바로 뒤에 붙이면
"82%977"로 읽혀 무슨 값인지 알 수 없다. 반드시 **구분자(가운뎃점)와 단위**를 넣는다 —
`<em> · 977건</em>`. CSS `margin-left`만으로는 안 된다 — innerText·복사·스크린리더에
간격이 전달되지 않는다. 실제 공백 문자를 넣는다.

## 유튜브·인스타 검색어는 넓히면 늘어난다 (2026-08-25 실측)

가전 낱말이 든 검색어/해시태그를 8→26개(유튜브), 4→18개(인스타)로 늘렸더니
유튜브 54→97편, 인스타 69→176건으로 늘었다. **기간을 못 고르는 채널은 검색어 수가
곧 표본 크기다.** 검색어는 반드시 가전 낱말이나 제품명을 포함해야 한다 —
"혼수 준비" 같은 넓은 말만 쓰면 예식장·드레스가 끌려와 걸러내는 데만 힘이 든다.

## 기간을 바꿔도 안 변하는 카드 — 원인은 "고정 CD 값을 직접 읽음" (2026-08-25)

> "기간 설정에 따라 분석 내용이 달라야 하는데 일부 변동이 없는게 보여"

**매니저 실명 카드(`mgrCard()`)가 기간과 무관하게 완전히 고정**돼 있었다.
1월·8월·전체 모두 "21/50 · 전국 40%"로 동일. 원인: `CD.mgr`/`CD.mgrStore`
(빌드 시점 전체 기간 고정값)를 직접 읽고 있었다 — 다른 카드는 전부
`hasF() ? A() : CD` 패턴으로 선택 기간을 반영하는데 이 카드만 예외였다.

**정작 데이터는 이미 기간별로 만들어져 있었다.** `scripts/build_web_data.py`가
매니저 실명 언급을 날짜 인덱스 팩트 테이블(`nm_prepped`)로 이미 인코딩해 두었고,
`web/fact.js`의 `VFACT.agg()`가 이를 디코드해 `R.mgr`/`R.mgrStore`를 기간별로
정확히 만들어 내고 있었다(실측: 1월 삼성실명 522건 vs 8월 304건, 완전히 다름).
**빌드·디코드 인프라는 맞았는데 화면(cafe-analysis.js)만 그걸 안 쓰고 있었다.**

`lg` 카드("품목별 이탈 현황")의 "실명 후기(구조적 열세)" 문단도 같은 원인으로
`CD.mgr`를 직접 읽어 똑같이 고정돼 있었다.

**교훈 — 새 카드/블록을 만들 때:**
- 숫자를 넣기 전에 `hasF() ? A() : CD` (또는 `perData()`) 패턴을 쓰는지 확인한다.
- "왜 기간이 안 바뀌지"가 의심되면, 먼저 데이터가 애초에 기간별로 존재하는지
  (`window.VFACT.agg(start,end)`를 콘솔에서 직접 호출)를 확인한 뒤 화면 코드를 고친다 —
  데이터가 없어서인지 화면이 안 쓰고 있어서인지는 원인이 다르다.
- `grep -n "CD\.\w\+" web/cafe-analysis.js`로 `hasF()`/`perData()` 가드 없이
  `CD.*`를 직접 읽는 곳을 주기적으로 훑는다.

## 매장 우위/열세 — 한쪽만 있어도 있는 쪽이 우위다 (2026-08-25 정정)

> "한쪽만 있는 경우 그 있는 쪽이 우위 아니야? 예를 들면 1:0이라는 말인데
> 그러면 1인쪽이 우위잖아."

2026-08-21에는 "삼성 후기가 매장명을 더 자주 적어 1:0 매장을 우위로 세면
표본 편향을 성적표로 둔갑시킨다"는 이유로 한쪽만 있는 매장을 **집계에서
제외**했다. 사용자가 이번에 다시 판단해 **있는 그대로의 건수 비교**로
되돌렸다 — 편향 자체는 위 `.nsc-bias` 문구로 이미 밝혀 두었으니, 매장
집계는 사실(건수)만 말하고 해석은 읽는 사람 몫으로 둔다.

**바뀐 것:** `winCount()`— 분모가 "양쪽 다 있는 매장"에서 "표본이 있는
매장(정규화 명부 기준)"으로, 분자도 1:0/0:1 매장을 포함해 계산한다.
분모 표기도 "명부 71곳 중 비교 가능 N곳"에서 **"전체 백화점 71개점"** 으로
바꿔, 그 71개점 전부를 기준으로 우위/열세 개점을 말한다.

**이 판단은 뒤집힐 수 있다.** 같은 사용자가 전에는 반대로 판단했었다 —
다음에 다시 바뀌면 이 항목을 그때 지시대로 고친다.

## 설명 문구는 사실만, 판단은 남기지 않는다 (2026-08-25)

> "'매장별 건수 차를 바이럴 우위로 읽지 마세요'는 의미가 있어 이런 단편적인
> 부분은 알아서 판단할 부분이야 삭제해줘."

편향 사실(회수율 삼성 7.7% vs LG 3.8%) 자체는 남기되, "그러니 이렇게 읽지
마라"는 지시문은 뺐다. **사실 고지와 해석 지시는 다르다** — 이 하네스는
사실은 반드시 밝히되(숫자를 지어내지 않는다), 그 사실을 어떻게 읽을지는
회사 사람들이 스스로 판단하게 둔다. 새 문구를 쓸 때 "~하지 마세요/~해야
합니다" 류의 지시문이 들어가면 정말 필요한지(예: 안전·규정 관련) 먼저
따진다.

## 카드 앞면도 긍정=파랑·부정=빨강 (2026-08-25 확장)

> "우측 카드 위 텍스트나 카드에서 상세로 진입한 내용에서 부정적이거나
> 부족한 것에 대해서는 글자색상을 빨간색으로 표현해줘."

앞서(같은 날 앞선 커밋) 상세 본문(`.fc-dbody`)에는 긍정=파랑·부정=빨강 원칙을
넣었는데, **카드 앞면(미니 수치·칩)은 빠져 있었다.** `.fc-mini b`와 `.fc-key`가
항상 파랑 고정이라 "열세 61%"·"LG 우위 격차 1,360건" 같은 부정적 수치도
앞면에서는 파랗게 보였다.

`fcard(key, label, title, mini, miniLab, detail, keys, miniNeg)`에 8번째
인자 `miniNeg`를 추가했다. `keys` 배열도 문자열(중립·파랑 유지, 하위호환)과
`{t, neg}` 객체(neg=true면 빨강)를 섞어 쓸 수 있다. **새 카드를 만들 때
앞면에 부정적 수치가 나오면 반드시 이 방식으로 색을 지정한다** — 그냥
문자열을 넣으면 파랑이 기본값이라 조용히 원칙을 벗어난다.

## 용어 — 은유·구어체보다 문어체 (2026-08-25)

> "새는 품목이 뭐야? 좀 문어적 표현으로 변경해줘."

"새는 품목"(누수 은유)처럼 비유적 표현은 뜻을 바로 못 읽는다. 이미 있는
"우위/열세" 어휘로 짝을 맞췄다 — "우위 품목" / "열세 품목". 카드 제목을
"OO 형성 요인"·"OO 요인 분석"처럼 문어체로 통일한 것과 같은 방향이다.
새 라벨을 지을 때 은유(새다·터지다·뚫리다 등)보다 "우위/열세/이탈/격차"처럼
이 하네스에서 이미 쓰는 문어체 어휘를 우선한다.

같은 지적이 반복됐다(2026-08-27, 2회): "갈라 셌습니다"·"마케팅 물량"이
뜻이 안 읽힌다 — 대시보드·마케팅 문서의 표준 용어로 쓴다. 코드 주석은 놔두되
화면에 나가는 문자열은 아래 치환표를 따른다.

| 쓰지 말 것 | 쓸 것 |
|---|---|
| 갈라 세다/보다/읽다 | 별도 집계 / 구분 집계 / ~기준으로 집계 |
| (글을) 마케팅 물량 | 체험단·협찬 **게시물** ('물량'은 글에 안 쓴다) |
| 글을 뿌리다 | 협찬 마케팅을 **집행**하다 |
| 검색 화면을 쥐다 | 검색 상위 노출을 **점유**하다 |
| 글이 덮다 | 비중이 높아 노출이 가려지다 |
| 옮길 교본 | 적용할 **모범 사례** |
| 바이럴의 관문 | 바이럴의 **핵심 거점** |
| 글이 몰리다 | 게시물이 **집중**되다 |
| 읽힘/읽히다(글) | **조회수**(2026-08-25 확정) · 검색 노출 지속 |
| 읽히다(영상) | **재생·조회수** |
| LG가 조용한 구간 | 경쟁사 **노출이 적은** 구간 |

검사법: `grep -n "물량\|뿌리\|쥐고\|덮고\|교본\|몰리\|읽힘\|읽히" web/*.js | grep '\`'`
— 백틱(템플릿 문자열)이 있는 줄만 화면에 나간다.

## "조회수"가 맞는 용어다 (2026-08-25)

> "hit건수라고 하니(읽은 사람) 다른표현이 있을 거야."

"읽은 사람 수"(순 방문자)로 오해되기 쉬운 "hit"/"읽힘" 대신 **"조회수"**를
쓴다 — 같은 사람이 여러 번 열어도 더해지는 누적 횟수라는 뜻을 정확히
전달하면서, 순 방문자 수라고 과장하지도 않는다. 후기 건수(post count)와
조회수(view count) 둘 다 같은 표기 형식 "브랜드 N건/회(퍼센트%)"를 쓴다
(`.nsc-vs2` 클래스, `web/cafe-analysis.css`).

## 전국 좌측 칼럼 = 제목 있는 섹션 박스 4개 (2026-08-25 사용자 지시)

> "제목을 크게 하고 해당 타이틀별로 박스화 하던지 해서 내용 구분을 해주자.
> 우위 열세점도 제목을 먼저 표시하고. 제일 아래 지역 요약도 동일한 양식과 흐름으로."

좌측 칼럼은 `후기 건수 · 조회수 · 매장 우위·열세 · 우위·열세 지역` 네 섹션이
같은 박스(`.nsc-sec`) + 같은 제목(`.nsc-st`, 14px, 점선 밑줄, 우측 보조라벨 `<i>`)을 쓴다.
새 블록을 추가하면 이 패턴을 따른다 — 제목 먼저, 내용은 그 아래.

**박스로 옮길 때 조심할 것 (실측 사고 둘):**
- 예전 스타일이 옛 래퍼 클래스(`.nsc-win …`)에 묶여 있으면, 래퍼를 갈아끼우는 순간
  숫자가 평문으로 풀린다. 스타일을 `.nsc-sec` 기준으로 다시 걸었다.
- 이 칼럼의 실제 폭은 264px 다. 일곱 자리 조회수 두 개를 한 줄에 나란히 두면
  박스를 28px 뚫는다 — 브랜드 줄(`.nsc-vs2`)은 **두 줄 세로 쌓기**가 기본이다.

## 화면 제목은 어느 단계든 26px (2026-08-25 통일 완료)

전국 26px vs 지역(부산)·매장(롯데 부산본점) 38.2px 로 갈라져 있었다
("매번 이야기 하는데 통일할 수 있는 요소는 모두 통일하자").
`analysis-ui.css` 의 통일 목록에 `.ca2 .rv-head h3` 를 추가해 전국=지역=매장=채널
모두 `--an-title`(26px) 하나다. 카드 소제목 `.ca-ch` 도 12.8/15/16.6 세 가지였던 것을
16.6px 하나로 못박았다. **새 화면·새 카드의 제목은 이 두 규칙에 자동으로 걸리는지
(analysis-ui.css 목록에 포함되는지) 만들 때 확인한다.**

## 분석글은 최소 2줄 — 축약이 해석을 막으면 실패다 (2026-08-25 사용자 지시)

> "각 분석글은 최소 2줄로 작성을 해줘. 너무 축약하니 무슨 말인지 모르겠어.
> 특히 후기는 8월 최대, 8월 최저는 해석 필요."

실측 사고: 성수기 문장이 `후기는 ${peak}월 최다, ${low}월 최저` 템플릿이라
**한 달만 선택하면 최다와 최저가 같은 달**이 되어 "8월 최다, 8월 최저 — 1.0배"
같은 무의미한 문장이 나왔다. 비교형 템플릿을 쓸 때는 **비교가 성립하지 않는
경계(단일 월·표본 1개·동률)에서 무슨 문장이 나오는지** 반드시 확인하고,
그 경우엔 비교 대신 안내("기간을 넓히면 성수기가 보입니다")로 갈아탄다.

카드 요점 칩(fc-key)도 "우위 52%"·"최저 0%" 같은 수치 조각이 아니라
**내용을 유추할 수 있는 문구**("삼성 비중 52% 접전"·"매장 간 편차 0~86%"·
"의류관리기 이탈 최다")로 쓴다. 명사+동사/판정어가 들어가야 칩만 보고도
카드 내용이 짐작된다.

## 좌측 막대 = 삼성 왼쪽(파랑)·LG 오른쪽(빨강) (2026-08-25)

후기 건수·조회수 막대는 브랜드명을 막대 **위 양 끝**, 수치를 **아래 양 끝**에 둔다
(`.nsc-ends` / `.nsc-nums`). 이름+수치를 한 덩이로 좌우 배치하면 칼럼 실폭
264px 에서 일곱 자리 조회수가 뚫린다 — 이름과 수치를 위아래로 가르면 들어간다.

## ±0 에는 판정어를 붙이지 않는다 (2026-08-25 사용자 지적)

> "같은 기간 대비 +0% 밀린다는 글은 무슨 뜻이야? 0%면 그냥 같은 수준인건데."

`${gap >= 0 ? "+" : ""}${gap}p ${유리/밀림}` 식 이지선다 템플릿은 gap=0 에서
"+0p 대비 밀립니다" 같은 모순 문장을 만든다. 실측으로 여섯 곳에서 나왔다
(비교 상담·직전 대비·전국 대비 강세/약세·요점 칩 등). **비교 수치 문장은
반드시 삼지선다(>0 / <0 / =0)** 로 쓰고, 0이면 "같은 수준"이라고만 말한다.
추이를 볼 수 없는 설정(단일 월)이면 돌려 말하지 말고 "월별 추이를 확인할 수
없습니다"라고 바로 말한다.

## 우위·열세 지역은 지도의 설명이다 (2026-08-25)

좌측 칼럼이 아니라 **지도 아래 범례 곁**(`.geo-rsum`)에 둔다 — 지도를 읽는
보조 설명이기 때문. 좌측 칼럼은 후기 건수·조회수·매장 우위·열세 세 박스로,
각 박스 안은 좌(삼성·우위=파랑) / 우(LG·열세=빨강) 정렬을 지킨다.
숫자 크기 상한: 이 칼럼(264px)에서 일곱 자리 조회수 두 개는 13.5px + 자간
-0.02em 이 한계다 — 14px 로 키우면 뚫린다(실측 재확인).

## 지역 페이지도 전국 양식을 따른다 (2026-08-25 사용자 지시)

> "다른 지역 페이지도 전국 양식을 참고하여 작업해줘. 다만 지역은 좀 더
> 디테일해야하고 매장별 현황도 봐야하는 만큼 이 부분 고려하고, 더 인사이트를
> 찾아내도록 깊이감 있게 분석해야 해."

지역 좌측 칼럼 = 전국과 같은 섹션 박스 문법(`.nsc-sec`/`.nsc-st`, 좌 삼성·파랑 /
우 LG·빨강) + 지역만의 디테일 두 가지:
- **매장 우위·열세** 섹션 안에 최강/공략 픽(클릭 시 매장 진입)
- **전국 안에서의 위치** 섹션(표본 규모 순위·전국 점유) — 도시 카드에서 승격

주인공은 여전히 **매장별 경쟁력 목록**(진단 3줄 상한·2열 규칙 유지).
도시 카드에는 **조회수 관점** 섹션을 추가했다 — 후기 수 1위와 조회수 1위가
다른 매장이면 "바이럴 파급은 후자가 크다"를 짚는다. 건수와 읽힘은 다른 축이다.

지역 조회수 합계는 **매장 특정 후기 기준**임을 라벨에 밝힌다 — 지역 전체
조회수라고 말하면 과장이 된다.

**±0 원칙의 연장 — 자명하거나 무의미한 문장 두 가지를 더 잡았다:**
- 열세가 1곳뿐인데 "격차의 100%를 차지" → "유일한 열세 매장"으로
- 격차가 작아 반올림 비중이 안 변하는데 "60% → 약 60%가 됩니다" →
  "후기 요청을 꾸준히 걸면 따라잡을 수 있는 거리"로.
  전환 효과 문장은 **projected > current 일 때만** 쓴다.

## 지역 페이지는 명부(백화점 리스트)를 분모로 말한다 (2026-08-25 사용자 지시)

> "부산지역 백화점은 5개점이고 그 가운데 우위 열세를 표현하는게 더 낫지 않을까?
> 아예 후기가 없는 매장은 후기가 나오도록 하던지, 혼수를 팔도록 노력을 하던지
> 이런 인사이트도 찾아낼수 있는거고."

- 지역 매장 목록·우위/열세 분모 = **명부(compete.json) 그 지역 매장 수**.
  후기 0건 매장도 숨기지 않고 "후기 0건 — 구매 고객 후기 요청부터" 행(`.rv-row.none`)으로 드러낸다.
- **명부의 `team` 필드는 영업팀이라 시도 구분이 안 된다**(부산·울산 매장이 전부 '경남' 팀).
  시도 매핑은 `rosterOfRegion()`(cafe-analysis.js) — 전 기간 CD.stores 의 매장→지역을
  이름(nrm0)으로 잇는다. 전 기간 후기 0건이라 지역을 못 잇는 4곳(현대부산·롯데메종동부산점·
  대백프라자·현대동구)은 폐점·특수관 이력이라 제외 — 사용자 기준 "부산 5개점"과 일치.
- 명부 매장은 표본 하한(floor) 필터에서도 빼지 않는다 — 명부는 SSOT.
- 좌측 칼럼 폭은 전국·지역 공통 `clamp(230px,20vw,288px)` (analysis-ui.css, ≥1181px) —
  페이지 전환 시 좌측 경계가 튀지 않게 두 그리드의 1열을 같은 식으로 고정했다.
- 도시 3열 카드에 전국 주제의 지역판(실명 언급·비교 상담) 추가 — 표본 10건 미만이면
  퍼센트 대신 건수로만 말한다. 매장 특정 후기가 0건인 구간의 액션은 "선두(삼성 -%)" 가
  되지 않게 별도 분기("매장이 특정된 후기가 없는 구간")를 탄다.

## 섹션 제목은 질문형이 아니라 문어체 명사구 (2026-08-26 사용자 지적)

> "부산은 어떤 도시인가는 잘 어울리지 않아 … 도시 안에서 얼마나 벌어져 있나 등
> 이해 어려운 문장과 표현이 많아. 실제 인사이트를 찾을 수 있는 표현을 사용해서 만들어."

- 카드·섹션 제목은 "~인가/~있나" 질문형·구어체("쏠림") 금지 — 기존 카드 어휘
  ("우위 형성 요인"·"품목별 경쟁 현황")와 같은 **문어체 명사구**로:
  매장 간 격차 · 표본 집중도 · 조회수 파급 · 실명 언급 · 비교 상담 · 품목 편차.
- 본문은 지표 설명이 아니라 **매장 실명·품목·다음 행동**이 든 문장으로. 분기마다
  (격차 큼/작음, 최하위도 우위, 집중/분산) 문장이 달라야 한다 — 승패가 뒤바뀌어도
  같은 문장이면 인사이트가 아니다.
- 이런 지적이 반복되지 않게: 문구를 고치면 **전 지역(14곳)×복수 기간 전수 감사**를
  스크립트로 돌리고(금지 패턴 정규식 + 좌측 칼럼 이탈 + 스크롤 발생), 마지막에
  instruction-steward 로 체크리스트 독립 검수를 받은 뒤 커밋·푸시·배포 확인까지 한다.

## 매장 후기 원문은 census 동적 샘플이다 (2026-08-26)

수작업 5개 매장×3건 고정 목록(SAMPLES)이 기간과 무관하게 붙박여 있었다
("기간을 변경하면 후기가 최신화 되어야지" — 사용자 지적 2회). 이제
`build_web_data.py` 가 매장 매칭 시 제목·작성일·조회수를 함께 담아
`CD.storeSamples`(110개 매장)로 내보내고, 화면(storeView)이 선택 기간으로
잘라 우호 3·주의 2건을 보여준다. 월별 상한은 **브랜드별**(삼성/LG 각
우호 2·주의 1) — 조회수 상위만 남기면 회수율이 높은 삼성이 달 전체를
차지해 LG 원문이 사라진다. 검색 API 의 `<b>` 태그는 빌드에서 걷어낸다.
기간 안 샘플이 없으면 최신 글을 보여주되 "가장 최근 후기"라고 밝힌다.
옛 SAMPLES 는 빌드 데이터에 없는 매장의 폴백으로만 남았다.

## 부정(주의) 판정은 낱말이 아니라 문맥이다 (2026-08-26 사용자 지적)

"후회 없으실 거예요"(긍정)가 '후회' 낱말 매치로 주의에 올랐다. 전수 실측:
후회 987건 중 650건이 "후회 없-", '하자' 709건 중 9할이 "졸업하자" 청유 어미,
'지연'은 사람 이름, "늦게까지 상담"·"예약 안 했으면 기다렸겠다"는 칭찬.
`NEG_RE`(build_web_data.py)를 문맥 가드로 재작성해 2,471→1,114건(55% 오탐 해소).
**교훈: 한국어 감성 키워드는 ①부정의 부정(없/않/안) ②동형이의어(명사 하자 vs
어미 -하자) ③가정문을 반드시 가드한다.** 새 키워드를 추가할 때는 census 전수로
매치 문맥을 뽑아 오탐률부터 실측한다(이 파일의 스크립트 패턴 재사용).

## 공유 컨테이너(#channelPanel)의 클릭 위임엔 화면 가드가 필수 (2026-08-26)

유튜브에서 기간 칩을 누르면 **카페(다이렉트웨딩) 화면이 대신 그려지는** 오류의
원인: cafe-analysis 가 #channelPanel 에 단 클릭 위임이 [data-per] 를 처리해
버렸다 — 그 컨테이너는 유튜브·인스타 등 모든 채널 화면이 공유한다.
고친 법: 핸들러 첫 줄에서 `document.body.classList.contains("view-cafe")` 가드.
**새 화면이 공유 컨테이너에 위임을 달 때는 반드시 자기 view-* 클래스를 가드한다**
(VPER.bind 는 칩 묶음(.vper)에만 달아 원래 안전).

## 하단 잘림은 두 종류다 — 뷰포트 밖 + overflow:hidden 안 (2026-08-26 전수 검수)

"하단에 짤려서 내용이 안 보이는 경우" 전수 검수 스크립트 두 개:
①잎 요소의 bottom>innerHeight(뷰포트 잘림) ②overflow hidden/clip 컨테이너의
scrollHeight>clientHeight(내부 잘림). 실측 적발: 서울 진단(.rv-diag) 54px,
매장 액션(.sv-action) 9px — cafe-analysis.css 의 (min-width:1180px) 블록이
.sv-action 을 hidden 으로 묶고 있었다(선택자 특이도가 높아 analysis-ui 로는
못 이김 — **원본 규칙에서 빼는 것**이 답). 원칙: **잘리느니 얇은 안쪽 스크롤
(overflow:auto)** — 페이지 스크롤(한 화면 원칙)과 다르다.

## 유튜브 화면 구성 (2026-08-26 재편)

- **첫 화면 = 최근 3개월** — `VPER.setInit(a,b,"최근 3개월")` (period.js 에 신설,
  fire 안 함·칩 누르면 평소 동작 복귀). 라벨 있는 초기 구간이 필요한 채널은 이걸 쓴다.
- 많이 본 영상은 **공식 채널 / 유튜버 두 카드로 갈라 순위**(.yt-podium 썸네일).
  기간 안에 없으면 비워 두지 않고 **전체 기간 상위 + 정직 문구** 폴백.
- 종합 진단(.yt-rep .cy-sec 박스 4개): 공식 채널 동향(삼성 n편 v회 vs LG) ·
  유튜버 게시물 동향(표본<10이면 편수만) · 고객 관심 트렌드(품목) · 최다 조회 영상 성격.
  전부 **보고서형 문어체**(사실→해석→시사점).
- 좌측 h3 = "재생 현황"(문어체). 요약 문단에 공식/유튜버 편수·조회수를 함께 적는다.

## 유튜브 2차 확장 — 성능·리뷰 축과 트렌드 요약 (2026-08-26)

- 수집이 **두 층**이다(collect_youtube.py): 혼수 검색어(가전∧혼수 낱말 필수) +
  `PERF_QUERIES` 20개(성능·비교·신모델 — 혼수 낱말 면제, **가전 낱말은 필수**).
  총 352편. 병합은 id 중복 제거, 조회수는 화면 표기값 그대로.
- 빌드(build_youtube_web.py)가 `cat:"spec"`(성능·리뷰 전문)을 분류한다 —
  전문 채널명(잇섭·리뷰머신·노써치…) ∨ 제목 어휘(화질·흡입력·소음·가성비·vs…).
  공식 채널은 spec 에서 제외(own 축과 겹치지 않게).
- 화면 좌측 = **트렌드 요약 불릿**(최다 조회·신모델/행사 언급·전문 영상 규모·관심 품목
  — 전부 제목·조회수 실데이터에서만, 사건을 지어내지 않는다) + **바이럴 대비**
  (공식/유튜버 × 삼성/LG 편수·조회수·막대). 우측 = 순위 카드 3장(공식/유튜버/전문,
  서로 중복 없음) + 종합 진단 5단락.

## 모든 분석 페이지는 "실행 제안 — 역할별"로 끝난다 (2026-08-26 사용자 지시)

> "결론적으로 해당 데이터 결과를 가지고 앞으로 어떻게 해야 하는지 그걸 제시해주길
> 바래. 교육이 될 수도 있고 판촉행사가 될 수도 있고 … 지역단위 영업팀, 매장,
> 혹은 본사에서의 역할도 각각 다를거야. … 앞으로 새롭게 생성될 페이지에서는
> 기본적으로 이 내용을 제시해줘."

- 표준 블록 = `.role-plan`(analysis-ui.css) — **본사 / 영업팀 / 매장** 세 줄 칩.
  각 줄은 그 페이지의 실데이터에서 도출한 **수단이 명시된** 문장(교육·판촉·후기
  요청 캠페인·콘텐츠 기획·대안 모델 지원 등)이어야 한다. 일반론 금지.
- 현재 위치: 전국(비교 상담 카드 상세 끝) · 지역(심층 분석 카드 끝) ·
  매장(현장 액션 카드 — 매장/영업팀/본사 순) · 유튜브·인스타(종합 진단 끝).
- **새 분석 페이지를 만들면 이 블록을 기본 포함한다.** 데이터 분기(우위/열세,
  표본 유무)마다 문장이 달라야 하고, 소표본이면 퍼센트 대신 건수 원칙을 따른다.

## 네이버 블로그 채널 (2026-08-26 신설)

- **수집 = `scripts/collect_blog.py`** (Playwright 스크래퍼 — 검색 API 는 신규 발급
  중단·SECRET 미등록이라 못 쓴다). 통합검색 블로그 탭을 검색어 36개 × 월 윈도우로
  정주행, `.done` 재개 지원. 2026-01~08 총 30,940건(내돈내산 3만·협찬표기 512).
- 빌드 = `build_blog_web.py` → `web/assets/blog.js`(window.NBLOG).
  **전량 posts 를 실으면 4.4MB** — 수치는 월별 사전집계(mon/monItems/monStores)
  전량으로, 글 목록만 월별 최신 표본(내돈내산 30·협찬 15)으로 담아 147KB.
  날짜 무효(범위 밖) 글은 빌드에서 제외 — 부제와 기간 합산이 어긋나지 않게.
- 화면 = `blog-view.js`(openBlog). 조회수가 없어 목록은 **최신순**(순위 지어내지
  않음), 체험단·협찬은 갈라 세고 브랜드 비교는 **내돈내산만**. 진단 5단락 +
  실행 제안(역할별). 매장 축 = store-scope `blogCard()`.
- 아이콘 = 초록(#03C75A) 바탕 + 흰 소문자 'b'(공식 문법) — 타일·머리글 공통(VICON).

## 블로그도 3단 드릴이다 (2026-08-26 사용자 지시)

blog-view 가 다결처럼 **전국(지도)→지역→매장**으로 드릴한다.
- 지도 = `window.KOREA_SIDO` 재사용(코로플레스·클릭 드릴). 지역 축은
  제목·요약 기반 **추정**, 매장 축은 지점명 기준 — 각주로 밝힌다.
- 빌드가 monRegions·storeRegion(지점 토큰→시도)·storePosts(매장별 최신 12)를
  추가로 싣는다. 드릴 상태(stv)는 기간과 독립 — 기간을 바꿔도 단계 유지.
- 조사 헬퍼 주의: blog-view 의 josa 는 **조사만** 돌려준다(이름을 함께 돌려주는
  cafe 의 josa 와 다름) — "은 이 기간" 사고의 원인이었다.

## 매장 매칭은 세 겹 가드를 거친다 (2026-08-26 오탐 청소)

`dept_store_of()`(build_web_data.py)의 오탐 3종을 전량 diff(census 12.1만+blog 3.1만)로
청소했다 — **매장 건수를 가르는 함수이므로 고칠 때마다 같은 공정(전/후 전량 diff
→ 방향 눈검수 → 재빌드)을 반드시 거친다.**
1. **비백화점 유통 마스킹**(NON_DEPT_RE): 하이마트·전자랜드·이마트·롯데마트·
   아울렛·한샘·무풍갤러리·갤럭시·AKOR 등 문구를 지점 표기까지 통째로 제거
   ("롯데하이마트 센텀시티점"이 롯데 센텀시티로 잡히던 사용자 지적).
2. **근접 지점 선택**: 체인 **뒤** 토큰 우선 → '점' 접미 우선 → 근접순
   ("성남 AK플라자 분당점"=분당, "안양평촌점"=평촌).
3. **이름·낱말 가드**: 토큰 뒤 호칭(매니저/매니져/명장/님…)이면 사람 이름
   (김수완·안동욱), 앞글자가 한글에 붙고 '점'으로 안 이어지면 낱말 조각
   (혼수완료의 '수완') — 문맥은 윈도가 아니라 **원문 기준**으로 본다.
정본 정규화: 신세계/갤러리아 천안아산점 → 천안(명부), 부산센텀 → 신세계 센텀시티.

## 한 화면 원칙 → 세로 스크롤 허용으로 전환 (2026-08-26 사용자 지시)

> "원페이퍼라고 했지만 하단으로 내용이 내려가면 안보이는 것을 방지하기 위해
> 브라우즈의 세로탭을 이용하도록"

`body.view-channel #channel.section` 의 `height:100dvh + overflow:hidden` 을
`min-height:100dvh + overflow:visible` 로 바꿨다(analysis-ui.css). 내부에서
내용을 잘라 쓰던 안쪽 스크롤(.rv-diag·.sv-action·.city-card·.yt-rep 등)도 함께 풀었다.
**이제 검수 기준이 바뀐다** — scrollHeight ≤900 은 더 이상 보지 않고,
①가로 스크롤 0 ②overflow hidden 내부 잘림 0(line-clamp·말줄임 제외) ③에러 0 을 본다.
우측 카드의 **높이 균일은 유지**(사용자 명시 요청) — 푼 것은 카드 '내부' 박스다.

## 오늘의집 채널 (2026-08-26 신설 — 채널마다 묻는 것이 다르다)

- **수집 = 실프로필·헤디드 브라우저**(`collect_ohou.py`). 헤드리스는 전 경로 403 —
  8/24 기록의 "로그인해도 차단"은 정책이 아니라 **헤드리스 지문 차단**이었다.
  통합검색(`/search/index?query=`)이 정본. 콘텐츠는 `contents.ohou.se/.../card_collections/{id}`,
  집들이는 `/projects/{id}`. 전용 피드는 검색어를 안 먹는다.
- **매장 드릴·기간 탭을 일부러 만들지 않았다** — 매장 단서가 사실상 없고(집들이 3/182)
  게시일도 비공개다. 없는 축·없는 정밀도를 화면에 만들지 않는다는 원칙이고,
  그 사실을 화면 문구로 밝힌다. 대신 **모델·공간 트렌드**가 이 채널의 축이다
  (비스포크 18·키친핏 16·오브제 16 / 주방 60·거실 25).
- 썸네일은 지연로딩 자리표시자(1x1 base64)만 잡혀 싣지 않는다 — 글 첫 문장을 제목으로 쓴다.

## 활자 통일의 근본 원인은 `body.view-cafe` 하드코딩이다 (2026-08-26 UI 검수)

`cafe-analysis.css` 956~1120 줄에 `body.view-cafe .OOO { font-size }` 가 ~150개 있고
**다결에만** 걸린다. 그래서 같은 컴포넌트가 블로그·유튜브·인스타(view-cx)·
네이버리뷰(view-nr)에서 기본값으로 나와 화면마다 25% 갈렸다(매장명 13~17.2px).
지금은 analysis-ui.css 가 `!important` 로 덮어 통일했지만 **두 곳이 싸우는 상태**다 —
항구적으로는 그 블록을 지우고 `--an-*` 토큰으로 대체해야 한다(다음 과제).

## 오늘의집 — 날짜·공감은 상세를 열어야 나온다 (2026-08-26 보강)

검색 격자에는 날짜도 반응도 없다. **상세 페이지 본문 끝**의
`07.05좋아요45스크랩36조회63` 한 덩어리가 유일한 단서다(`enrich_ohou.py`).
- 연도가 없어 "그 월·일이 오늘보다 미래면 작년"으로 보정 — **추정이므로 화면에 밝힌다**.
- 사이드바의 `2024.09.08 ~ 2027.09.07` 은 **사업자 정보 기간**이지 게시일이 아니다
  (세 글 모두 같은 값이라 실측으로 걸렀다) — 이걸 썼으면 전 글이 같은 날짜가 됐다.
- 좋아요(공감)·스크랩(사러 갈 목록)·조회수는 **뜻이 다르다**. 갈라서 싣고,
  화면에서도 "저장이 좋아요에 근접할수록 구매 의향"으로 해석한다.
실측: 182건 중 날짜 171(93%)·반응 182(100%), 월 분포 2026-06 이 63건으로 최다.

## 분석 페이지의 좌·우 역할 (2026-08-26 사용자 지시)

> "좌측 캡션은 요약 정리, 우측 분석 부분은 전체 진단 글(총론)을 써주고
> 각론으로 들어가는 구조로"

- **좌측** = 요약 — 타이틀 박스 · 총량 · 핵심 수치 박스(2~3개) · 요약 불릿 · 방법론 각주.
- **우측** = 진단 — **총론**(2~3문단: 이 기간 전체를 한 문단으로 요약 → 채널 성격 →
  기간 특징) 다음에 **각론 ①②③④**(공감·모델·공간·광고 등 축별), 끝에 실행 제안(역할별).
- 각론 제목에 번호를 붙여 총론과 위계가 눈에 보이게 한다.

## 활자 계단 현재값 (2026-08-27 갱신 — 1.2배 확대 반영)

사용자 지시("분석페이지의 글씨크기를 지금보다 1.2배는 크게")로 계단을 다시 잡았다.
**`web/analysis-ui.css` 한 곳에만 둔다.**

| 토큰 | 값 | 굵기 | 쓰는 곳 |
|---|---|---|---|
| `--an-title` | 31px | 900 | 화면 제목(전국·지역·매장·채널 모두 같다) |
| `--an-cardt` | 20px | 900 | 카드 제목 |
| `--an-sect` | 16.5px | 800 | 소제목(`.cy-sec h5`·`.nsc-st`·`.af-sec>h4`) |
| `--an-body` | 15.5px | 600 | 본문 |
| `--an-foot` | 13.5px | 600 | 각주 |

**새 화면을 만들면 그 화면의 선택자를 이 목록에 넣는다.** 넣지 않으면 조용히
계단 밖에 남는다 — 네이버 리뷰(`.nr-wrap`)와 매장별 현황(`.cx-card`)이 12.5~14.5px로
흩어져 있던 것을 2026-08-27에야 찾았다.

## 기간 UI 는 화면마다 같은 자리·같은 한 벌 (2026-08-27 사용자 지시)

> "모든 분석 페이지에서 기간 버튼탭을 우측으로 이동 / 간단 기간설정 버튼과
> 직접입력 기간 버튼 모두가 있어야 / 기간과 분석결과는 서로 연동되어"

- `VPER.bar()` 하나가 **간단 칩 + 직접 입력**을 함께 낸다. `html()` 만 쓰면
  칩만 나와 직접 입력이 빠진다 — 새 화면은 반드시 `bar()`.
- 자리는 **우측**. `.ca-perwrap { margin-left: auto }` 로 통일했고, 다결처럼
  머리줄에 칩과 입력이 갈라져 있던 화면은 둘 다 우측으로 붙였다.
- **연동은 눈으로 확인한다.** 기간을 바꿔도 수치가 그대로면 UI 만 단 것이다.
  네이버 리뷰 목록이 그랬다(누적 총계를 그대로 세고 있었다) — `cnt()` 로
  선택 기간의 행만 세도록 고치고, 각주도 "누적 총계 / 선택 기간의 수집 표본"으로 갈아탄다.
- 기간 라벨 표기는 **하이픈**(`2026-03-01 ~ 2026-05-31`). `fact.js` 만 점을
  찍고 있어 같은 기능이 화면마다 달라 보였다.

## 실행 제안 블록이 빠진 화면이 없는지 훑는다 (2026-08-27)

"모든 분석 페이지는 실행 제안으로 끝난다"는 표준을 세운 뒤에도 제이웨딩·
네이버 리뷰(목록·상세)·매장별 현황 **네 곳이 빠져 있었다.** 새 화면을 만들거나
검수할 때 `grep -l "role-plan" web/*-view.js web/store-scope.js` 로 목록을 훑는다.

문장은 그 화면의 실데이터에서 나와야 한다:
- 매장별 현황 — 카드가 '표본 없음'으로 그려진 채널을 모아 **공백 채널을 실명으로** 지목
- 네이버 리뷰 상세 — 상대 매장에만 있는 **칭찬 키워드**를 짚어 교육 항목으로 연결
- 네이버 리뷰 목록 — 격차가 가장 큰 백화점을 실명으로

## 표본 목록의 길이를 전량 집계인 척 적지 않는다 (2026-08-27)

제휴카페 분류 열 제목이 `fmtN(list.length)` — 화면에 뿌린 **표본 6건**을 적고
있었는데 좌측 요약은 전량 17건이라 같은 화면 안에서 숫자가 어긋나 보였다.
제목에는 **전량 집계**를 적고, 표본 수는 `표본 N건` 으로 따로 밝힌다.

## 매장별 현황은 채널 허브다 — 합산·주요 채널·클릭 진입 (2026-08-27)

- 좌측 큰 숫자 = **노출된 채널 전부의 합**(`chCounts()`, store-scope.js).
  "수집된 채널 합산"이라 적어 놓고 다결만 세던 사고의 재발 방지 —
  새 채널을 붙이면 `chCounts()`에도 한 줄 추가해야 좌측 합계에 들어간다.
- 주요 채널 상위 3을 칩으로, 채널 카드는 전부 클릭 → 그 채널의 매장 화면.
  매장 축이 있는 채널은 직행 API(`openCafeStore`·`openBlogStore`·`openNaverReview`),
  없는 채널(제이웨딩·유튜브·인스타·오늘의집)은 채널 화면으로.
- 카드 클릭 래퍼는 `display:contents`(`.cx-gowrap`) — 그리드가 안 깨진다.
- 브랜드 비중 막대는 삼성/LG가 갈리는 채널만 합산(리뷰·인스타 제외), 순위
  KPI는 웨딩카페 표본 기준 — 둘 다 각주로 명기했다. 숫자의 근거가 섞이면
  합계가 어느 채널 것인지 알 수 없게 된다.
- 오늘의집 카드는 "수집 대기"가 아니라 **매장 축 없음**(182건 수집됨) —
  없는 축을 지어내지 않되, 수집 안 했다는 오기는 더 나쁘다.

## 전월비 추세 화살표 — 절대 건수가 아니라 페이스로 판정 (2026-08-27)

매장별 현황의 채널 카드에 주식 시세판풍 배지(`.cx-trend`)를 단다.
**당월은 진행 중이라 절대 건수로 전월과 비교하면 항상 하락으로 오독된다** —
그래서 `moTrend()`(store-scope.js)가 당월을 경과일로 환산한 월말 페이스로
전월과 견주고 방향만 표시한다.

- 판정: 페이스 ≥ 전월×1.15 → 상승 ▲(파랑) / ≤ ×0.85 → 하락 ▼(빨강) / 사이 → 보합 ―(회색)
- 색은 한국 주식 관례(상승=빨강)가 아니라 **이 하네스의 원칙(긍정=파랑·부정=빨강)** 을 따른다
- 소표본 가드: 전월·페이스 둘 다 3건 미만이면 방향을 단정하지 않고 보합 + 문구
- 근거는 title 툴팁에: "전월 N건 → 이번 달 M건(페이스 환산 약 P건)"
- 2026-08-27 확장: 제이웨딩(addDate)·인스타(taken_at)·유튜브(상대 표기 환산 추정)도
  빌드가 `monStores`(ym→매장→수치)를 실어 **여섯 채널 모두** 기간 연동·화살표가 된다.
  유튜브만 게시월이 추정이라 카드에 "게시월 추정" 배지 + 각주를 단다("N년 전"은
  월 정밀도가 없어 월 집계에서 제외). 새 채널을 붙이면 빌드에 monStores 를 싣고
  store-scope 의 `monAgg()` 를 재사용한다

## 네이버 리뷰 매장 상세 — 추세·당사/경쟁사 분석·요인 (2026-08-27 재편)

- **리뷰 추세**: 기준월(자체 타임라인 > 표준 기간 끝 월 > 데이터 최신 월)
  직전 6개월을 우리/상대 듀얼 막대로. 요약은 전·후반 3개월 합의 삼지선다 +
  상대 흐름 교차 문장. 표본 6건 미만이면 단정하지 않는다.
- **당사/경쟁사 리뷰 분석**(`reviewAnalysis`): 감성(칭찬 태그 총량·최다 축·아쉬움) →
  방문 사유(사유별 접점 처방) → 품목. **품목은 원문이 수집된 표본(아쉬움 56건)에서만**
  읽는다 — 네이버가 리뷰 원문을 공개 집계하지 않는다는 한계를 문장에 명시.
- **우세·열세 요인**: 기간 표본(A.n vs B.n)으로 판정하고 이유를 데이터에서 찾는다 —
  예약 경유율 차(≥3p)·표본당 칭찬 밀도·방문 사유 축 차이·실명 언급 유무.
  · 한쪽 표본 10건 미만 → 비율 요인을 들이대지 않고 "회수 자체가 과제"로.
  · **판정 방향과 문장 방향을 맞춘다** — 밀리는데 우리 지표가 높으면
    "격차의 원인은 이것이 아니라는 뜻" 분기로(우세 톤 문장이 나오면 모순).
- 숫자 뒤 '(으)로'는 `numRo()`(끝자리 소리 기준, 1·7·8은 ㄹ이라 '로') —
  "74으로" 같은 오류 방지.

## 다결 좌측에 전국 혼인 건수 참조 (2026-08-27 신설)

수집 총량("N건 분석") 아래 `.nsc-marry` 한 줄 — 선택 기간의 **전국 혼인 건수**
(시장 규모의 눈금). 데이터 = `scripts/build_marriage_web.py` → `web/assets/marriage.js`.

- **공식치** = 통계청(국가데이터처) 인구동향 월별 보도자료(2024~2026 발표분 14개월,
  출처 URL 은 빌드 스크립트 머리 주석에 전부 기록) + 연간 확정(2021~2025).
- **추정치** = 미발표 월. 2021~2025는 "연간 공식 − 발표월 합"의 잔여 균등 배분
  (연간 검산 assert 로 합이 공식과 일치함을 빌드가 보증), 2026 미발표월은
  전년 동월 × 발표월 평균 증감률(+4.2%).
- 라벨이 구성에 따라 바뀐다: 전부 공식 → "통계청 인구동향" / 섞임 → "· 일부 추정"
  / 전부 추정 → "추정". 상세 방법은 title 툴팁.
- 새 월 발표가 나오면 빌드 스크립트 OFFICIAL 에 한 줄 추가하고 재빌드한다.
